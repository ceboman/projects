{% extends 'base.html.twig' %}

{% block body %}
    <h1>Creación Area</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('area_index') }}">Regresar</a>
        </li>
    </ul>
{% endblock %}
