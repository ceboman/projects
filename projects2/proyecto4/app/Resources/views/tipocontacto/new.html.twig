{% extends 'base.html.twig' %}

{% block body %}
    <h1>Creación Tipo Contacto</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('tipocontacto_index') }}">Regresar</a>
        </li>
    </ul>
{% endblock %}
