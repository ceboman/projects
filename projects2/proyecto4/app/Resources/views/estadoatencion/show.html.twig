{% extends 'base.html.twig' %}

{% block body %}
    <h1>Estado de Atención</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ide stadoatencion</th>
                <td>{{ estadoAtencion.idEstadoAtencion }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ estadoAtencion.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('estadoatencion_index') }}">Regresar</a>
        </li>
        {%  if is_granted('ROLE_ADMIN') %}
        <li>
            <a href="{{ path('estadoatencion_edit', { 'idEstadoAtencion': estadoAtencion.idEstadoAtencion }) }}">Editar</a>
        </li>
        {% endif %}
        {%  if is_granted('ROLE_ADMIN') %}
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
        {% endif %}
    </ul>
{% endblock %}
